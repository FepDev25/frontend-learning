// Métodos de los String: toUpperCase, toLowerCase, toString

const producto = "Monitor de 20 pulgadas";

console.log(producto.toUpperCase());
console.log(producto.toLowerCase());

const number = 12;
console.log(number); // como number
console.log(number.toString()); // El método toString() devuelve una cadena que representa al objeto Number.
