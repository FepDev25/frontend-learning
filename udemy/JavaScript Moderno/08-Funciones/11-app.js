// Funciones flecha con un solo parámetro y retorno implícito

const sumar5 = (num) => num + 5;
console.log(sumar5(10));

const operar = (a, b) => {
    const c = a*b;
    return (a + b)/c;
};

console.log(operar(9, 67));