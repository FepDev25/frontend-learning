// Operador ternario

const autenticado = true;
console.log(autenticado ? "Autenticado" : "No autenticado");

const num1 = 400;
const frase = num1 > 350 ? "Number mayor a 350" : "Number menor a 350";
console.log(frase);

const num2 = 200;
const mensajePar = num2 % 2 === 0 ? "Es par" : "Es imparr";
console.log(mensajePar);
